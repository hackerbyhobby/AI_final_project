# Security-alerted packages bumped to versions that resolve all open
# Dependabot alerts (incl. the gradio cookie-injection GHSA-7hp7-4p35-3cx2).
gradio==6.19.0
transformers==5.12.1
torch==2.12.1
pillow==12.3.0
httpx==0.28.1
sentencepiece==0.2.1

# No open alerts; unchanged.
pytesseract==0.3.10
langdetect==1.0.9
deep-translator==1.10.1

# Not imported by app.py; unpinned so the resolver can satisfy the
# numpy/scipy constraints of the bumped torch/transformers.
numpy
shap

# Pre-1.0 openai code (openai.ChatCompletion) needs migration to the >=1.0
# client API; left unpinned pending that separate change.
openai
